<!DOCTYPE html>
<html lang="es">
	<!-- include Head -->
	<%- include("./partials/head") %>

	<body>
		<!-- Header -->
		<% if (locals.user != undefined) { %>
			<%- include("./partials/headerLogueado") %>
		<% }else {%>
			<%- include("./partials/header") %>
		<%} %>

		<!-- /Header -->

		<!-- Novedades -->
		<div class="container products-wrapper">
			<div class="row">
				<div class="col-12">
					<h2 class="products-title">Ingresar</h2>
				</div>
				<!-- Show the "ultimos" products -->
				
				<form action="/users/login" method="POST" class="search-form" >
					<input id="email" type="email" name="email" placeholder="Email" class="formulario">
					<input id="contrasenia" type="password" name="contrasenia" placeholder="Contraseña" class="formulario">
					<input id="rememberme" type="checkbox" name="rememberme" placeholder="Recordarme" class="checkbox">					
					<button type="submit" class="enviar">Enviar</button>
				</form>
			</div>	
		</div>
		<!-- footer -->
		<%- include("./partials/footer") %>

		<!-- /footer -->
	</body>

</html>