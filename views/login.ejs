<!DOCTYPE html>
<html lang="es">
	<!-- include Head -->
	<%- include("./partials/head") %>

	<body>
		<!-- Header -->
		<% if (locals.user != undefined) { %>
			<%- include("./partials/headerLogueado") %>
		<% }else {%>
			<%- include("./partials/header") %>
		<%} %>

		<!-- /Header -->

		<div class="container products-wrapper">
			<div class="row">
				<div class="col-12">
					<h2 class="login">Ingresar</h2>
				</div>
				<h2>
					<form action="/users/login" method="POST" class="search-form" >
						<input id="email" type="email" name="email" placeholder="Email" class="formulario">
						<input id="contrasenia" type="password" name="contrasenia" placeholder="Contraseña" class="formulario">
						<label for="rememberme" class="Recordarme"> Recordarme</label><input id="rememberme" type="checkbox" name="rememberme" placeholder="Recordarme" class="checkbox">
						<button type="submit" class="enviar">Enviar</button>
					</form>
				</h2>
			</div>	
		</div>
		<!-- footer -->
		<%- include("./partials/footer") %>

		<!-- /footer -->
	</body>

</html>