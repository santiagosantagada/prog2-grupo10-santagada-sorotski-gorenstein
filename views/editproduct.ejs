<!DOCTYPE html>
<html lang="es">
	<!-- include Head -->
	<%- include("./partials/head") %>

	

	<body>
		
		<!-- /Header -->
		<% if (locals.user != undefined) { %>
			<%- include("./partials/headerLogueado") %>
		<% }else {%>
			<%- include("./partials/header") %>
		<%} %>
	

=		<div class="container products-wrapper">
			<div class="row">
				<div class="col-12">
					<h2 class="login">Modificar producto</h2>
				</div>
				<h2 > 
					<form action="/product/editproduct/<%= datos.id%>" method="POST" class="search-form" >
						<input type="text" name="foto" placeholder="<%= datos.foto %>" class="formulario" value="<%=locals.old != undefined && old.foto ? old.foto:""%>">
						<% if (locals.errors && errors.foto) { %>
							<p><%= errors.foto.msg %></p>
						   <% } %>
						<input type="text" name="nombreProducto" placeholder="<%= datos.nombreProducto %>" class="formulario" value="<%=locals.old != undefined && old.nombreProducto ? old.nombreProducto:""%>">
						<% if (locals.errors && errors.nombreProducto) { %>
							<p><%= errors.nombreProducto.msg %></p>
						   <% } %>
						<input type="text" name="descripcion" placeholder="<%= datos.descripcion %>" class="formulario" value="<%=locals.old != undefined && old.descripcion ? old.descripcion:""%>">
						<% if (locals.errors && errors.descripcion) { %>
							<p><%= errors.descripcion.msg %></p>
						   <% } %>

						<button type="submit" class="enviar">Modificar</i></button>
					</form>

				</h2>
			</div>
		</div>

		<!-- footer -->
		<%- include("./partials/footer") %>

		<!-- /footer -->
	</body>

</html>